apiVersion: v1
kind: Service
metadata:
  name: student-dashboard-service
spec:
  type: NodePort
  selector:
    app: student-dashboard
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
